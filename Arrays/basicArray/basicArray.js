'use strict';

// create a function to add values into an empty array

funtion addValuesArray(array){
    let sum = [];
    for(let i = 0; i < 100; i++){
        sum[i] = i + 1;
    }
    return sum;
}